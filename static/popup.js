function popup(surveyId) {
    //var popup = document.createElement("div");
    var linkText = document.location.origin + "/survey/" + surveyId.toString();
    //var link = document.createTextNode(linkText);
    //popup.appendChild(link);
    //popup.classList.add('link');
    //document.getElementById("link").appendChild(popup);
	alert(linkText);
}
